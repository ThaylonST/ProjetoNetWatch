import { View, Text } from "react-native";

export default function DeviceList() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Lista de dispositivos aparecerá aqui</Text>
    </View>
  );
}
